<div class="flatscha flatscha-toggle-switch @(this.Checked ? "checked" : "")" @onclick="this.Clicked">
    <div class="slider" />
    @if (!string.IsNullOrWhiteSpace(this.Label))
    {
        <div class="switch-label">@this.Label</div>
    }
</div>